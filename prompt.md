# 游戏人生 APP - 零基础生成指南

> 不会写代码？不会用 GitHub？没关系！
> 只要会复制粘贴，跟着下面的步骤走，你就能拥有一个属于自己的日程管理 APP。

---

## 第一步：选择一个 AI 编程工具

以下工具任选一个即可，推荐用 **TRAE**（免费、效果好、支持中文）：

| 工具 | 网址 | 特点 |
|------|------|------|
| **TRAE**（推荐） | https://www.trae.cn | 免费、中文界面、生成质量高 |
| Cursor | https://cursor.com | 付费、专业级 |
| ChatGPT | https://chat.openai.com | 需付费版才能写代码 |
| 通义灵码 | https://tongyi.aliyun.com | 免费、中文 |

> **以 TRAE 为例**：打开网址 → 注册登录 → 新建对话 → 把下面的提示词粘贴进去 → 发送

---

## 第二步：复制以下提示词

将下方框内的所有文字**全部复制**，粘贴到 AI 工具的对话框中：

---

```
我想做一个手机端的个人日程管理 PWA 应用，零依赖纯 HTML/CSS/JS，单文件，支持离线使用和安装到桌面。设计风格用莫兰迪灰粉配色（低饱和度，温柔低调），不要用动物森友会风格。

### 功能需求

**整体架构：**
- 纯 HTML/CSS/JavaScript，不依赖任何框架或库
- PWA 支持（manifest.json + Service Worker 离线缓存）
- localStorage 数据持久化
- 手机端全屏 APP 模式，适配刘海屏/安全区域
- 左侧导航栏（中文标签），右侧内容区

**五大模块：**

1. **工作台**
   - 欢迎语 + 日期 + 天气
   - 今日待办 + 近期待办列表
   - 语音/手动添加任务，自动识别时间和紧急程度（紧急/重要/普通）
   - 完成任务获得积分（紧急200/重要150/普通100）
   - 飞书多维表格同步（可选功能，可开关）

2. **养生日常**
   - 日常习惯打卡（喝水、八段锦、梳头发、早睡等，可自定义添加/删除）
   - 运动记录（跑步、游泳、跳舞、瑜伽等，可自定义添加/删除）
   - 每种运动有注意事项和教程
   - 记录运动时长，按时长计算积分
   - 习惯打卡每次30积分

3. **学习**
   - 书架管理（书名、作者、阅读理由、计划完成时间、阅读进度打卡）
   - 课程管理
   - 笔记记录
   - 打卡获得100积分

4. **心愿板**
   - 添加心愿目标（如旅行、购物等）
   - 设置所需积分
   - 用积分享兑换
   - 已兑换/待兑换状态

5. **成就清单**
   - 可视化展示所有完成的任务、习惯、运动、心愿
   - 统计总积分、完成任务数等
   - 成就徽章系统

**积分系统：**
- 工作任务：紧急200/重要150/普通100
- 运动记录：按时长计算（每10分钟10积分，上限300）
- 习惯打卡：每次30积分
- 学习打卡：每次100积分
- 积分可在心愿板兑换奖励

**个人设置：**
- 可修改昵称
- 可选择应用图标（预设 emoji + 自定义上传）
- 飞书同步开关

**设计要求：**
- 莫兰迪灰粉配色：背景 #F0E6E8，卡片 #EDE0E3，主色 #B08585
- 圆角卡片设计，柔和渐变
- 手机端适配刘海屏（safe-area-inset）
- 桌面端用手机框架预览
- 顶部安全区用深色莫兰迪色条
- Manrope 字体

**PWA 配置：**
- manifest.json（standalone 模式、竖屏）
- Service Worker 离线缓存
- 支持 iOS Safari 添加到主屏幕
- 支持 Android Chrome 安装提示
```

---

## 第三步：等待 AI 生成代码

发送提示词后，AI 会开始编写代码，通常需要 1-3 分钟。

- AI 会生成一个完整的 `index.html` 文件
- 可能还会生成 `manifest.json` 和 `sw.js` 两个小文件
- 如果 AI 没有一次生成完，可以发"继续"让它接着写

> **提示**：如果生成过程中出现问题，可以直接告诉 AI，比如"语音功能不能用"、"颜色不对"等，它会帮你修改。

---

## 第四步：保存文件

### 方法一：在 TRAE 中保存（推荐）

1. AI 生成代码后，点击代码右上角的"复制"按钮
2. 在电脑上新建一个文件夹（比如叫 `我的APP`）
3. 用记事本（Windows）或文本编辑器（Mac）新建文件
4. 把代码粘贴进去，保存为 `index.html`
5. 如果 AI 还生成了 `manifest.json` 和 `sw.js`，同样保存到同一文件夹

### 方法二：直接下载

如果 AI 工具支持文件下载，直接下载所有文件到同一文件夹即可。

---

## 第五步：在手机上安装使用

### 方法 A：通过数据线传输（最简单）

1. 用数据线把手机连到电脑
2. 把刚才保存的文件夹复制到手机里
3. 在手机上用浏览器打开 `index.html`

### 方法 B：通过微信发送

1. 把 `index.html` 文件通过微信发给自己
2. 在手机上点击文件 → 用浏览器打开

### 方法 C：如果你有网盘

1. 把文件上传到百度网盘 / iCloud / Google Drive
2. 在手机上下载
3. 用浏览器打开

### 安装到手机桌面（变成 APP）

**苹果手机 (iPhone)：**
1. 用 **Safari 浏览器** 打开 `index.html`（必须用 Safari，其他浏览器不行）
2. 点击底部 **分享按钮**（方框向上的箭头 ↑）
3. 滑动找到 **"添加到主屏幕"**
4. 点击 **"添加"**
5. 桌面上就会出现 APP 图标，点开就是全屏 APP

**安卓手机 (Android)：**
1. 用 **Chrome 浏览器** 打开 `index.html`
2. 点击右上角 **三个点**（菜单）
3. 选择 **"添加到主屏幕"**
4. 或者等浏览器自动弹出安装提示，点击 **"安装"**
5. 桌面上出现 APP 图标

---

## 想要更多功能？

生成 APP 后，你可以继续跟 AI 说，让它修改或添加功能。比如：

- "帮我把配色改成蓝色系"
- "添加一个记账功能"
- "加一个番茄钟计时器"
- "修改习惯打卡的积分为 50 分"
- "加一个每日打卡日历视图"
- "加一个密码锁功能，打开 APP 需要输入密码"

直接用大白话说就行，AI 会帮你改代码。改完后重新保存 `index.html` 文件，刷新手机上的页面即可。

---

## 想要离线使用？

如果你希望 APP 在没有网络时也能用（真正的离线 APP），需要：

1. 确保保存了 `index.html`、`manifest.json`、`sw.js` 三个文件
2. 这三个文件放在同一个文件夹里
3. 通过 HTTPS 网址访问（GitHub Pages、网盘链接等均可）
4. 安装到手机桌面后，即可离线使用

> 如果你只是在本手机上用，不追求离线功能，只要一个 `index.html` 文件就够了。

---

## 常见问题

**Q：打开后是白屏怎么办？**
A：换一个浏览器试试，推荐用 Safari（iPhone）或 Chrome（Android）。

**Q：数据会丢失吗？**
A：数据存在你手机的浏览器里，只要你不清除浏览器缓存就不会丢。但如果换了手机，数据不会自动迁移。

**Q：别人能看到我的数据吗？**
A：不会。所有数据都在你自己的手机本地，不会上传到任何服务器，完全隐私。

**Q：可以同步到其他设备吗？**
A：默认不支持。提示词里有一个可选的"飞书同步"功能，需要自己对接飞书 API。如果不需要可以忽略。

**Q：不想用莫兰迪配色，能换吗？**
A：当然可以！在提示词里把配色部分改成你喜欢的颜色就行，比如：
- "设计风格用清新绿色系，背景 #F0F5EE，主色 #7BAE9E"
- "设计风格用高级黑白灰，背景 #F5F5F5，主色 #333333"
- "设计风格用温暖橘色系，背景 #FDF0E6，主色 #D4915C"

---

## 总结流程图

```
复制提示词 → 粘贴到 AI 工具 → 等待生成代码
                                    ↓
                              保存为 index.html
                                    ↓
                        传到手机 → 用浏览器打开
                                    ↓
                        添加到主屏幕 → 变成 APP
```

就这么简单，动手试试吧！
